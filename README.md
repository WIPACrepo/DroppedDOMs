# DroppedDOMs
iOS app to list the dropped DOMs for a run

To build:
* clone this repo
* start up XCode and point it at this project
* build the app and install it on your favorite iOS device
* enjoy!
